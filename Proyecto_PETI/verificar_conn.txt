if __name__ == "__main__":
    conn1 = Conexion()
    conn2 = Conexion()

    print(f"Objeto conn1: {conn1}")
    print(f"Objeto conn2: {conn2}")
    print(f"¿Son conn1 y conn2 la misma instancia? {'Sí' if conn1 is conn2 else 'No'}")

    # También puedes comprobar si comparten la misma conexión a la base de datos
    print(f"Conexión de conn1: {conn1.get_connection()}")
    print(f"Conexión de conn2: {conn2.get_connection()}")
    print(f"¿Son las conexiones de conn1 y conn2 la misma? {'Sí' if conn1.get_connection() is conn2.get_connection() else 'No'}")

    # Cerramos la conexión para limpiar
    conn1.close_connection()